<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MP_FinalProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="android:fontFamily">@font/lato_regular</item>
        <item name="android:textColor">@color/primary_black</item>
        <item name="colorAccent">@color/primary_blue</item>
        <item name="colorPrimaryContainer">@android:color/transparent</item>
        <item name="colorSecondaryContainer">@color/primary_blue</item>
        <item name="colorControlNormal">@color/primary_black</item>
    </style>

    <style name="tags">
        <item name="android:textColor">@color/pale_white</item>
    </style>

    <style name="background">
        <item name="android:background">@color/pale_white</item>
    </style>

    <style name="button_blue">
        <item name="android:background">@drawable/rounded_button_solid</item>
        <item name="android:fontFamily">@font/lato_bold</item>
        <item name="android:backgroundTint">@color/primary_blue</item>
        <item name="android:textColor">@color/pale_white</item>
    </style>

    <style name="logo">
        <item name="tint">@color/primary_black</item>
    </style>

    <style name="Theme.MP_FinalProject" parent="Base.Theme.MP_FinalProject" />
</resources>